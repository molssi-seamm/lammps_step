name: test

channels:
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - rdkit
  - configargparse
  - matplotlib
  - py-cpuinfo
  - statsmodels

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - codecov
    - pillow
    - seamm-ff-util>=0.9
    - seamm_util>=0.9
    - seamm>=0.9
    - seamm-widgets>=0.9
    - packaging

      # Testing
    - yapf
