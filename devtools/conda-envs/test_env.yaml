name: test

channels:
  - rdkit
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  # SEAMM
  - kim-query
  - kim-query
  - matplotlib
  # molsystem requires, and only available via Conda.
  - openbabel
  - psutil
  - rdkit
  - statsmodels

  # Testing
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - pymbar
    - seamm
    - seamm-ff-util

      # Testing
    - yapf

    # Documentation
    - rinohtype
    - pygments
    - sphinx-rtd-theme
    - pystemmer
