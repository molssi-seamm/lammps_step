name: test

channels:
  - rdkit
  - default
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - rdkit
  - configargparse
  - kim-query
  - matplotlib
  - openkim-models
  - py-cpuinfo
  - statsmodels

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - codecov
    - pillow
    - seamm-ff-util>=0.9
    - seamm_util>=0.9
    - seamm>=0.9
    - seamm-widgets>=0.9
    - packaging
    - pymbar

      # Testing
    - yapf
