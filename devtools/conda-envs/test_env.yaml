name: test

channels:
  - conda-forge
  - defaults

dependencies:
  # Base depends
  - python
  - pip

  # SEAMM
  - kim-query
  - seamm
  - seamm-ff-util
  - statsmodels
  - tabulate

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
    - pymbar>=4.0
    - seamm-exec
    # Documentation
    - sphinx-copybutton
