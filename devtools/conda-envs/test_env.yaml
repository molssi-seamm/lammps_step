name: test

channels:
  - rdkit

dependencies:
    # Base depends
  - python
  - pip
  - rdkit
  - configargparse
  - matplotlib
  - py-cpuinfo
  - statsmodels

    # Testing
  - coverage
  - flake8
  - pytest
  - pytest-cov
  - pytest-runner

  # Pip-only installs
  - pip:
    - codecov
    - pillow
    - seamm-ff-util
    - seamm_util
    - seamm
    - seamm-widgets
    - packaging
    - pprint

      # Testing
    - yapf
